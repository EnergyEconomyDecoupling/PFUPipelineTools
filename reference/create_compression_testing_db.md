# Create a database for testing table compression

This function is used for testing.

## Usage

``` r
create_compression_testing_db(conn)
```

## Arguments

- conn:

  The connection to the database.

## Value

The `index_map` for the database.
