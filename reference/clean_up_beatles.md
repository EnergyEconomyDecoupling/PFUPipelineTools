# Clean up Beatles tables

Used only for testing.

## Usage

``` r
clean_up_beatles(conn)
```

## Arguments

- conn:

  The connection to a Postgres database. The user must have write
  permission.

## Value

A list of tables deleted
