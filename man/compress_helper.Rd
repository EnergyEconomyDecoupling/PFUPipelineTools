% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/local-compress.R
\name{compress_helper}
\alias{compress_helper}
\title{A local table compression helper function}
\usage{
compress_helper(
  remote_df,
  local_df,
  valid_from_version_colname = PFUPipelineTools::dataset_info$valid_from_version_colname,
  valid_to_version_colname = PFUPipelineTools::dataset_info$valid_to_version_colname,
  value_colname = PFUPipelineTools::mat_colnames$value,
  what_to_do_colname = PFUPipelineTools::dataset_info$what_to_do,
  change_valid_to_version_in_remote =
    PFUPipelineTools::dataset_info$replace_valid_to_version_in_remote,
  delete_row_in_remote = PFUPipelineTools::dataset_info$delete_row_in_remote,
  replace_value_in_remote = PFUPipelineTools::dataset_info$replace_value_in_remote,
  upload_new = PFUPipelineTools::dataset_info$upload_new,
  current_version_int = PFUPipelineTools::current_version_int,
  tol = 1e-06
)
}
\arguments{
\item{remote_df}{A remote version of the rows contained in \code{local_df}.}

\item{local_df}{A new data frame computed locally that
contains a new set of values for \code{remote_df}.}

\item{valid_from_version_colname, valid_to_version_colname}{See
\link{dataset_info}.
Defaults are \link{dataset_info}\verb{$valid_from_version_colname} and \link{dataset_info}\verb{$valid_to_version_colname} or
"ValidFromVersion" and
"ValidToVersion".}

\item{value_colname}{The name of the value column.
Default is \link{mat_colnames}\verb{$value} or
"value".}

\item{what_to_do_colname}{The name of the column that tells
what to do with the row.
Default is \link{dataset_info}\verb{$what_to_do} or
"WhatToDo".}

\item{change_valid_to_version_in_remote}{The string that
indicates the value in the remote's
\code{valid_to_version} column
should be changed.
Default is \link{dataset_info}\verb{$replace_valid_to_version_in_remote} or
"Replace ValidToVersion in remote".}

\item{delete_row_in_remote}{The string that indicates the row should be
deleted from the remote database.
Default is \link{dataset_info}\verb{$delete_row_in_remote} or
"Delete row in remote".}

\item{replace_value_in_remote}{The string that
indicates the value of remote's \code{value} column
should be changed.
Default is \link{dataset_info}\verb{$replace_value_in_remote} or
"Replace value in remote".}

\item{upload_new}{The string that
indicates local rows
to be uploaded to the remote.
Default is \link{dataset_info}\verb{$upload_new} or
"Upload new".}

\item{current_version_int}{The integer representing the current version.
Default is
\link{current_version_int} or
2147483647.}

\item{tol}{The tolerance within which a local value will be
assumed same as the remote value.
Default is \code{1e-6}.}

\item{i_colname}{The name of the column containing row indices for matrices.
Default is \link{mat_colnames}\verb{$i} or
"i".}

\item{j_colname}{The name of the column containing column indices for matrices.
Default is \link{mat_colnames}\verb{$j} or
"j".}
}
\value{
A data frame with same columns as \code{remote_df} and \code{local_df} and an
added column (\code{what_to_do_colname}).
}
\description{
We decide before uploading to the remote database
whether rows in the remote table can be compressed
via the use of \code{ValidFromVersion} and \code{ValidToVersion} columns.
This function contains the logic for that work.
}
\details{
In the context of this function,
\code{remote} means (sometimes older) data from the remote database.
\code{local} means new data calculated locally and meant to be uploaded
to the remote database.

This function doesn't change any rows in the remote database.
Rather, it returns a data frame with same columns as
\code{remote_df} and an additional column
named with the value of \code{what_to_do_colname}
(by default
\link{dataset_info}\verb{$what_to_do} or
"WhatToDo") that tells
what must be done with each row.
The possible values of the \code{what_to_do_colname} are
\link{dataset_info}\verb{$replace_valid_to_version_in_remote},
\link{dataset_info}\verb{$replace_value_in_remote},
\link{dataset_info}\verb{$delete_row_in_remote}, and
\link{dataset_info}\verb{$upload_new}
that indicate whether to change the remote table's
\code{ValidToVersion} value,
replace the value in the \code{value} column,
delete the remote row, or
upload a new row, respectively.
The values are
"Replace ValidToVersion in remote",
"Replace value in remote",
"Delete row in remote", and
"Upload new",
respectively.

Functions that call \code{\link[=compress_helper]{compress_helper()}} should query the value
of the \code{what_to_do_colname} to decide how to handle
each row.

If both \code{remote_df} and \code{local_df} are \code{NULL}, \code{NULL} is returned.
If only \code{remote_df} is \code{NULL}, all rows in \code{local_df} are assumed to require
uploading to the remote.
If only \code{local_df} is \code{NULL}, no rows need to be uploaded to the remote and
no rows in the remote need to be changed.

Note that both \code{remote_df} and \code{local_df} should be
encoded data frames, i.e.
their foreign key columns should all be ID integers.

Note: \code{local_df} should have the same values in both
\code{valid_from_version_colname} and
\code{valid_to_version_colname}
to clearly indicate intent by the caller.
If not, an error is thrown.

Important: \code{local_df} is assumed to contain
a complete set of information for metadata columns
(columns excluding \code{i}, \code{j}, and \code{value}).
Thus, if for the same metadata and the current version,
\code{local_df} is lacking some rows present in \code{remote_df},
those rows will be removed from \code{remote_df}.
}
\examples{
remote_df <- tibble::tribble(
  ~Dataset, ~ValidFromVersion, ~ValidToVersion, ~Country, ~Year, ~matname, ~i, ~j, ~value,
   5, 2, current_version_int, 49, 1971, 2, 1, 1, 11,
   5, 2, current_version_int, 49, 1971, 2, 1, 2, 12,
   5, 2, current_version_int, 49, 1971, 2, 1, 3, 13,
   5, 2, current_version_int, 49, 1971, 2, 2, 1, 21,
   # matname = 3 is U
   5, 2, current_version_int, 49, 1971, 3, 1, 1, 110,
   5, 2, current_version_int, 49, 1971, 3, 2, 2, 220,
   5, 2, current_version_int, 49, 1971, 3, 3, 2, 320,
   # Country = 146 is USA
   # matname = 7 is V
   5, 2, current_version_int, 146, 1972, 7, 1, 1, 1100,
   5, 2, current_version_int, 146, 1972, 7, 2, 2, 2200,
   5, 2, current_version_int, 146, 1972, 7, 3, 5, 3500,
   # matname = 8 is Y
   5, 2, current_version_int, 146, 1972, 8, 1, 1, 11000,
   5, 2, current_version_int, 146, 1972, 8, 2, 1, 21000,
   5, 2, current_version_int, 146, 1972, 8, 1, 2, 12000
)
# Change 2 rows
local_df <- remote_df |>
  dplyr::mutate(
    # Change the version information
    "{PFUPipelineTools::dataset_info$valid_from_version_colname}" := 3,
    "{PFUPipelineTools::dataset_info$valid_to_version_colname}" := 3,
  )
local_df[3, PFUPipelineTools::mat_colnames$value] <- -13
local_df[12, PFUPipelineTools::mat_colnames$value] <- -21000
# Returns only the rows that need to be changed
# and what must be done.
compress_helper(remote_df = remote_df, local_df = local_df)
}
