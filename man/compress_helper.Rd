% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/local-compress.R
\name{compress_helper}
\alias{compress_helper}
\title{A local table compression helper function}
\usage{
compress_helper(
  remote_df,
  local_df,
  valid_from_version_colname = PFUPipelineTools::dataset_info$valid_from_version_colname,
  valid_to_version_colname = PFUPipelineTools::dataset_info$valid_to_version_colname,
  value_colname = PFUPipelineTools::mat_colnames$value,
  what_to_do_colname = PFUPipelineTools::dataset_info$what_to_do,
  change_remote = PFUPipelineTools::dataset_info$change_remote,
  upload_new = PFUPipelineTools::dataset_info$upload_new,
  current_version_int = PFUPipelineTools::current_version_int,
  tol = 1e-06
)
}
\arguments{
\item{remote_df}{A remote version of the rows contained in \code{local_df}.}

\item{local_df}{A new data frame computed locally that
contains new values for \code{remote_df}.}

\item{valid_from_version_colname, valid_to_version_colname}{See
\link{dataset_info}.
Defaults are \link{dataset_info}\verb{$valid_from_version_colname} and \link{dataset_info}\verb{$valid_to_version_colname} or
ValidFromVersion and
ValidToVersion}

\item{value_colname}{See \link{mat_colnames$value}.}

\item{current_version_int}{The integer representing the current version.
Default is
\link{current_version_int} or
2147483647.}

\item{tol}{The tolerance within which a local value will be
assumed same as the remote value.
Default is \code{1e-6}.}
}
\value{
A list of two data frames
}
\description{
We decide before uploading to the remote database
whether rows in the remote table can be compressed
via the use of \code{ValidFromVersion} and \code{ValidToVersion} columns.
This function contains the logic for that work.
}
\details{
This function doesn't change any rows in the remote database.
Rather, it returns a data frame with same columns as
\code{remote_df} and an additional column
named with the value of \code{what_to_do_colname}
(default is "WhatToDo") that tells
what must be done with each row.
The possible values of the \code{what_to_do_colname} are

The first data frame (named "change_remote")
contains rows that need to be changed in the remote.
All columns should be the same as the remote, but
\code{valid_from_version_colname} and
\code{valid_to_version_colname} may be different.
The second data frame (named "upload_to_remote")
contains rows that need to be added to the remote table.

In the context of this function,
\code{remote} means (usually older) data from the remote database.
\code{local} means new data calculated locally and meant to be uploaded
to the remote database.
}
