% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/schema.R
\name{update_schema_table}
\alias{update_schema_table}
\title{Update a schema table in a database.}
\usage{
update_schema_table(
  dbname,
  db_table_name,
  input_data_version,
  project_path = file.path("~", "OneDrive", "OneDrive - University of Leeds",
    "Fellowship 1960-2015 PFU database research"),
  input_data_path = file.path(project_path, "InputData", input_data_version),
  schema_path = file.path(input_data_path, "SchemaAndFKTables.xlsx"),
  schema_sheet = "Schema",
  table_colname = "Table",
  is_pk_colname = "IsPK",
  colname_colname = "Colname",
  drv = RPostgres::Postgres(),
  host = "mexer.site",
  port = 6432,
  user = "dbcreator",
  conn = DBI::dbConnect(drv = drv, dbname = dbname, host = host, port = port, user =
    user)
)
}
\arguments{
\item{dbname}{The name of the database in which the table should be updated.}

\item{db_table_name}{The name of the table to update.}

\item{input_data_version}{A string representing the version of input data to be used.}

\item{project_path}{The path to the schema excel file.
Default is the OneDrive folder for this project.}

\item{input_data_path}{The path from \code{project_path} to the input data folder.}

\item{schema_path}{The path from \code{input_data_path} to the schema file.}

\item{schema_sheet}{The name of the schema sheet in the schema file.
Default is "Schema".}

\item{table_colname, is_pk_colname, colname_colname}{Names of the table,
isPK, and colname columns in the schema table.
Defaults are "Table", "IsPK", and "Colname", respectively.}

\item{drv}{The database driver to be used. Default is \code{RPostgres::Postgres()}.}

\item{host}{The host for the database. Default is "mexer.site".}

\item{port}{The port for accessing the database. Default is \code{6432}.}

\item{user}{The user for the database. Default is "dbcreator".}

\item{conn}{The connection to the database. Default is created from
\code{drv}, \code{dbname}, \code{host}, \code{port}, and \code{user} arguments.}
}
\value{
\code{TRUE} if successful
}
\description{
It is sometimes necessary to update a schema table in the database.
The need can arise when additional rows are added to the Index table,
for example.
This function helps with that process.
}
