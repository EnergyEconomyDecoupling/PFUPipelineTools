% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/storing.R
\name{do_upsert_and_compress}
\alias{do_upsert_and_compress}
\title{A helper function that performs the upsert and compress action}
\usage{
do_upsert_and_compress(
  df_to_upsert,
  valid_from_version_colname,
  valid_to_version_colname,
  row_colname = PFUPipelineTools::mat_colnames$row,
  col_colname = PFUPipelineTools::mat_colnames$col,
  value_colname = PFUPipelineTools::mat_colnames$value,
  what_to_do_colname,
  mat_colnames,
  remote_tbl,
  pk_str,
  tol,
  current_version_int,
  in_place
)
}
\arguments{
\item{df_to_upsert}{The data frame to be upserted.}

\item{valid_from_version_colname}{The string name of the valid from version column.
Cannot be \code{PFUPipelineTools::version_info$current_version_string} or
"current".}

\item{valid_to_version_colname}{The string name of the valid to version column.
Cannot be \code{PFUPipelineTools::version_info$current_version_string} or
"current".}

\item{row_colname}{The name of the column that indicates matrix row names.
Default is \link{mat_colnames}\verb{$row} or
"i".}

\item{col_colname}{The name of the column that indicates matrix column names.
Default is \link{mat_colnames}\verb{$col} or
"j".}

\item{value_colname}{The name of the column that indicates matrix values.
This argument could have length greater than 1,
in which case, all items in the vector are assumed to be value columns.
Default is \link{mat_colnames}\verb{$value} or
"value".}

\item{what_to_do_colname}{The string name of a column that tells what to do
with various rows of \code{.df}.
This column is used internally.}

\item{mat_colnames}{String names of columns in \code{.df} that contain matrix information,
namely, rowname (or index), colname (or index), and value.}

\item{remote_tbl}{A remote table against which \code{df_to_upsert} is compared.}

\item{pk_str}{The string for the primary key.}

\item{tol}{The tolerance within which a local value will be
assumed same as the remote value.
This value is passed to \code{\link[=compress_helper]{compress_helper()}}.}

\item{current_version_int}{An integer that indicates the current version in the remote table.}

\item{in_place}{A boolean that tells whether to change the remote table.}
}
\value{
Nothing useful.
This function should be called for its side effect of updating the remote table.
}
\description{
A helper function that performs the upsert and compress action
}
