% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/destroy.R
\name{pl_destroy}
\alias{pl_destroy}
\title{Reset the CL-PFU database pipeline to original condition}
\usage{
pl_destroy(
  conn,
  store = targets::tar_config_get("store"),
  destroy_cache = FALSE,
  drop_tables = FALSE
)
}
\arguments{
\item{conn}{A \code{DBI} connection to a database.}

\item{store}{The path to the \code{targets} store.
Default is \code{targets::tar_config_get("store")},
which is normally \verb{_targets}.}

\item{destroy_cache}{A boolean that tells whether to destroy the local \code{targets} cache.
Default is \code{FALSE}.}

\item{drop_tables}{A boolean that tells whether to delete all tables in \code{conn}.
Default is \code{FALSE}.}
}
\value{
If successful, \code{TRUE} (invisibly);
otherwise, an error is thrown.
}
\description{
Sometimes, you just need to start over from scratch.
This function destroys the local \code{targets} cache
and removes all tables in the database at \code{conn}.
Be sure you know what you're doing!
}
\details{
\code{conn}'s user must have superuser privileges.

Because this is a very destructive function,
the caller must opt into behaviors.

If \code{drop_tables} is \code{TRUE},  \code{destroy_cache} is implied to be \code{TRUE},
and the targets cache is destroyed.
}
