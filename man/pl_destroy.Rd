% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reset.R
\name{pl_destroy}
\alias{pl_destroy}
\title{Reset the CL-PFU database pipeline to original condition}
\usage{
pl_destroy(
  conn = DBI::dbConnect(drv = RPostgres::Postgres(), dbname = conn_args$dbname, host =
    conn_args$host, port = conn_args$port, user = conn_args$user),
  conn_params,
  store = targets::tar_config_get("store")
)
}
\arguments{
\item{conn}{A \code{DBI} connection to a database.
Default uses the value of \code{conn_params} to connect.
Override with your own connection.}

\item{conn_params}{A named list with items \code{dbname}, \code{host}, \code{port}, and \code{user},
corresponding to the parameters for \code{DBI::dbConnect()}.}

\item{store}{The path to the \code{targets} store.
Default is \code{targets::tar_config_get("store")},
which is normally \verb{_targets}.}
}
\value{
If successful, \code{TRUE}, otherwise, an error is thrown.
}
\description{
Sometimes, you just need to start over from scratch.
This function destroys the local \code{targets} cache
and removes all tables in the database at \code{conn}.
Be sure you know what you're doing!
}
