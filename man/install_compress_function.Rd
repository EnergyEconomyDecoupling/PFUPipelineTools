% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compress.R
\name{install_compress_function}
\alias{install_compress_function}
\title{Install the \code{compress} function to a database}
\usage{
install_compress_function(
  conn,
  compress_func_string = readr::read_file(system.file("extdata", "compress.sql", package
    = "PFUPipelineTools"))
)
}
\arguments{
\item{conn}{A connection to the database into which the \code{compress}
function should be added.}

\item{compress_func_string}{The compress function.
Default is
\verb{readr::read_file(file.path("data-raw", "compress.sql")}.}
}
\value{
The number of rows affected by adding the function to the
database at \code{conn},
which should always be \code{0}.
}
\description{
When running the pipeline,
we compress identical rows of the table using the version columns.
This plpgsql function in \code{compress_func_string}
does that work for us.
This function adds the \code{compress} function
to the database at \code{conn}.
}
\details{
If \code{compress} is already installed into the the database at \code{conn},
only the user who previously installed \code{compress} can update it.

To remove \code{compress}, see \code{\link[=remove_compress_function]{remove_compress_function()}}.
}
\seealso{
\code{\link[=remove_compress_function]{remove_compress_function()}}
}
