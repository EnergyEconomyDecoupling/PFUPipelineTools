% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/storing.R
\name{release_many_pins}
\alias{release_many_pins}
\title{Save many targets to a pinboard.}
\usage{
release_many_pins(
  pipeline_releases_folder,
  release_info,
  release = FALSE,
  targ_colname = "targ",
  pin_name_colname = "pin_name",
  targ_type_colname = "type"
)
}
\arguments{
\item{pipeline_releases_folder}{The folder that contains the pinboard for releases from the pipeline.}

\item{release_info}{A data frame information about these releases. See details.}

\item{release}{A boolean telling whether to do a release.
Default is \code{FALSE}.}

\item{targ_colname, pin_name_colname, targ_type_colname}{String names of columns in \code{release_info}.}
}
\value{
If \code{release} is \code{TRUE},
a list of fully-qualified path names of the pinboard items.
If \code{release} is \code{FALSE}, the string "Release not requested."
}
\description{
Releases (\code{release = TRUE})
or not (\code{release = FALSE})
new versions of targets
using the \code{pins} package.
}
\details{
Released versions of the target can be obtained
as shown in examples.

\code{release_info} is a \code{tibble} with columns
\itemize{
\item \code{targ} (a list column containing the data to be released),
\item \code{pin_name} (the string pin names for each target), and
\item \code{targ_type} (the string target type, typically "csv" or "rds").
}
}
\examples{
\dontrun{
# Establish the pinboard
pinboard <- file.path("~",
                      "Dropbox",
                      "Fellowship 1960-2015 PFU database",
                      "OutputData", "PipelineReleases") |>
  pins::board_folder()
# Get information about the `PSUT` target in the pinboard
pinboard |>
  pins::pin_meta(name = "psut")
# Find versions of the `PSUT` target
pinboard |>
  pins::pin_versions(name = "psut")
# Get the latest copy of the `PSUT` target.
my_psut <- pinboard |>
  pins::pin_read(name = "psut")
# Retrieve a previous version of the `PSUT` target.
my_old_psut <- pinboard |>
  pins::pin_read(name = "psut", version = "20220218T023112Z-1d9e1")
}
}
