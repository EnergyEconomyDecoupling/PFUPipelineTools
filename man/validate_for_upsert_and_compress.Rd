% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/storing.R
\name{validate_for_upsert_and_compress}
\alias{validate_for_upsert_and_compress}
\title{Validate and prepare the incoming data frame
for \code{upsert_and_compress()}.}
\usage{
validate_for_upsert_and_compress(
  .df,
  db_table_name,
  valid_from_version_colname,
  valid_to_version_colname,
  version_string,
  current_version_string
)
}
\arguments{
\item{.df}{The data frame being validated.}

\item{db_table_name}{A string identifying the destination for \code{.df} in \code{conn},
i.e. the name of a remote database table.
Default is \code{NULL}, meaning that the value for this argument
will be taken from the \code{.db_table_name} column of \code{.df}.}

\item{valid_from_version_colname}{The string name of the valid from version column.
Default is \link{dataset_info}\verb{$valid_from_version_colname} or
"ValidFromVersion".
Cannot be \code{PFUPipelineTools::version_info$current_version_string} or
"current".}

\item{valid_to_version_colname}{The string name of the valid to version column.
Default is \link{dataset_info}\verb{$valid_to_version_colname}
or
"ValidToVersion".
Cannot be \code{PFUPipelineTools::version_info$current_version_string} or
"current".}

\item{version_string}{An optional string that tells the version of the database being updated.
See details.
Default is \code{NULL}, meaning version information should be
obtained from the \code{ValidFromVersion} and \code{ValidToVersion} columns
of \code{.df}.}

\item{current_version_string}{A string that identifies the current version in the remote table.
Default is \code{PFUPipelineTools::version_info$current_version_string} or
current.
It is probably a \emph{very bad} idea to supply
a different value from the default.}
}
\value{
An updated version of \code{.df} ready for upserting and compressing.
}
\description{
Validate and prepare the incoming data frame
for \code{upsert_and_compress()}.
}
